/**
 * setupTests.js — Vitest global test setup.
 * Imports @testing-library/jest-dom so all matcher extensions (toBeInTheDocument, etc.)
 * are available in every test file without per-file imports.
 */
import '@testing-library/jest-dom';
