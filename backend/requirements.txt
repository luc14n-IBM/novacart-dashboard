fastapi==0.141.1
uvicorn[standard]==0.52.1
snowflake-connector-python==4.7.1
python-dotenv==1.2.2
# cryptography is intentionally unpinned — snowflake-connector-python owns this
# dependency and resolves it to a compatible version (currently 50.0.0).
# Pinning it separately would cause a conflict.
cryptography
pytest==9.1.1
# httpx is used by FastAPI's TestClient — keep pinned to latest 0.x stable
httpx==0.28.1
